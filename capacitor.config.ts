import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.twozeroone.thirdofficer',
  appName: '3rd Officer Checklist',
  webDir: 'dist',
};

export default config;
